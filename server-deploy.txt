cd /opt/my-site && git pull origin master && DOCKER_BUILDKIT=0 docker compose -f docker-compose.yml -f deploy/docker-compose.prod.yml up -d --build && docker exec my-site-backend-1 alembic upgrade head
